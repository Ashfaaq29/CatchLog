/** MapLibre style URLs — primary free stack with demo fallback. */
export const MAP_STYLE_PRIMARY = 'https://tiles.openfreemap.org/styles/dark';
export const MAP_STYLE_FALLBACK = 'https://demotiles.maplibre.org/style.json';

export const MAP_TOPO_TILES = ['https://tile.opentopomap.org/{z}/{x}/{y}.png'];
